var assert = require("assert");
var testObj = require("../src");

describe('ajax(thinkbase.net)', function() {

    describe('#post(url, dataObj, successCallback)', function() {
        it('Post json data to server, then run success callback', function() {
            assert.fail("", "", "Run `webpack-dev-server`, then browse `http://localhost:8080/test/test.html` to test in browser", "");
        });
    });

});
